package com.syslibrary.tests.borrowing_books;

public class BorrowBooksTests2 {
    /**
     Create a new student /add_user
     verify that there are entries for that student in the book_borrow in database
     create a new book using /add_book
     borrow that book for that student
     verify that there is one  entry for that student in the book_borrow in database
     Get borrowed books using /get_borrowed_books_by_user/{user_id}
     verify that db entry and the response payload has same information

     return that book
     verify that there is still one  entry for that student in the book_borrow in database
     Get borrowed books using /get_borrowed_books_by_user/{user_id}
     verify that db entry and the response payload has same information

     */
}
