package com.syslibrary.tests.user;

public class TokenTests {
    /**

     TOKEN TESTS
     Create a new student using /add_user
     Get token for that student using /login
     Verify that response contains fields: token and redirect_uri with strings values
     Convert JWT token to object data using /decode
     Verify json response has correct values
     **/
}
